@model HomeViewModel
<h1> Кращі моделі</h1>
<div class="row mt -5 mb-2">
    @{
        foreach (Product product in Model.favGoods)
        {
            @Html.Partial("AllShoes", product)
        }
    }
</div>